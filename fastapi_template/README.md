# fastapi_template

This is a template repository for APIs using FastAPI.


## Running the Project with Docker

```bash
docker build -t fastapi_template .
docker run -d -p 9000:8888 fastapi_template
```